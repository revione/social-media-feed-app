import styled from "styled-components/macro"

export const Nav = styled.nav`
  display: flex;
  background-color: #926bcf;

  color: white;

  padding: 0 20px;

  section {
    max-width: 799px;
    margin: 0 auto;
  }
`
